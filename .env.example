# ─── Sepolia Testnet ──────────────────────────────────────────────────────────
# RPC endpoint for Sepolia (public RPC used by default, but a private one is faster)
NEXT_PUBLIC_RPC_URL="https://ethereum-sepolia-rpc.publicnode.com"

# WalletConnect project ID (get one at https://cloud.walletconnect.com)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=""

# Sepolia relayer URL — absolute http(s) only. Unset = direct Zama testnet relayer.
# To use the same-origin proxy instead (recommended for production):
# NEXT_PUBLIC_RELAYER_URL="https://your-app.vercel.app/api/relayer/11155111"

# ─── Ethereum Mainnet ─────────────────────────────────────────────────────────
# RPC endpoint for Ethereum mainnet (public fallback: llamarpc.com)
NEXT_PUBLIC_MAINNET_RPC_URL="https://eth.llamarpc.com"

# Mainnet relayer URL — set when Zama issues a mainnet relayer API key.
# Routes through the same-origin proxy at /api/relayer/1 (keeps key server-side).
# NEXT_PUBLIC_MAINNET_RELAYER_URL="https://your-app.vercel.app/api/relayer/1"

# Mainnet relayer API key — server-side only (not NEXT_PUBLIC_).
# Used by /api/relayer/[...path]/route.ts when proxying mainnet relayer calls.
# MAINNET_RELAYER_URL="https://relayer.mainnet.zama.org/v2"
